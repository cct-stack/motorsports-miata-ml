# Runtime + test dependencies, pinned to the versions this project was
# validated against (reproducible builds). Bump deliberately, then re-run
# `pytest` to confirm the baseline lap times still hold.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
matplotlib==3.10.9
optuna[tqdm]==4.9.0
scikit-learn==1.9.0
openpyxl==3.1.5

# Test framework
pytest==9.0.3
